# Dépendances
node_modules/

# Build
dist/

# Caches Astro
.astro/

# Variables d'environnement locales (documentées dans .env.example)
.env
.env.*
!.env.example

# Réglages locaux Claude Code (propres à chaque machine)
.claude/settings.local.json

# Divers
*.log
.DS_Store

# impeccable — état de session (design.json et config.json restent suivis)
.impeccable/hook.cache.json
.impeccable/hook.pending.json
.impeccable/config.local.json
.icones-avant/
.vercel/
