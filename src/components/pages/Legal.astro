---
// TODO : compléter avec les informations réelles avant la mise en ligne.
// Ce rappel reste dans le frontmatter : un commentaire HTML serait servi au
// visiteur, ce qui n'a rien à faire sur une page juridique.
import DocumentLegal from '../DocumentLegal.astro';
import { splitPath } from '../../lib/i18n';
import { legal } from '../../i18n/legal';

const { lang } = splitPath(Astro.url.pathname);
---

<DocumentLegal texte={legal[lang]} doc="mentions-legales" />
